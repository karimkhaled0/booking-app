/* eslint-disable @next/next/no-img-element */
import { HomeIcon } from '@heroicons/react/24/solid'
import type { NextPage } from 'next'
import Head from 'next/head'
import BookFlight from '../components/BookFlight'
import LeftHeader from '../components/LeftHeader'
import TopHeader from '../components/TopHeader'

const Home: NextPage = () => {
  return (
    <div className='bg-[#EBF0F3] h-screen'>
      <Head>
        <title>KarimBook</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>


      <div>
        {/* Background Image */}
        <div className='h-96 bg-[#1a507c] relative'>

          {/* Sky image */}
          {/* FIXME: use next image */}
          <div className='w-full h-5'>
            <img src='/sky4.png' alt="s" className='w-full h-[500px] object-cover absolute bottom-28' />
          </div>

          {/* Plane Image */}
          {/* FIXME: use next image */}
          <div>
            <img src="/plane.png" alt="plane" className='absolute right-44 top-32' />
          </div>

          {/* Top Header*/}
          <TopHeader />

          {/* Left Header */}
          <LeftHeader
            home={true}
            wallet={false}
            booking={false}
            business={false}
            explore={false}
            support={false}
          />

          {/* Book Flight */}
          <BookFlight />

        </div>
      </div>

    </div>
  )
}

export default Home


// import React, { Suspense } from 'react';

// const OtherComponent = React.lazy(() => import('./OtherComponent'));

/* 
<Suspense fallback={<div>Loading...</div>}>
        <OtherComponent />
      </Suspense> 
*/